- tid = "graph-#{Time.now.usec}"

.line_graph{:"data-xset" => "#{xset}", :"data-yset" => "#{yset}", :"data-legend" => "#{legend}", :"data-xtype" => "#{xtype.to_s}", :"data-labels" => "#{labels}", :id => tid}

:javascript
  grapher.transform("#{tid}")